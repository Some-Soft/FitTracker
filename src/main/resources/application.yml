management:
    endpoints:
        web:
            base-path: /
            exposure.include: health
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/fittracker
        password: password
        username: user
